{% extends "layout.twig" %}
{% block title %}Cadastrar de produtos{% endblock %}

{% block content %}
    <div class="jumbotron">
        Cadastrar Produto


        <form action="#" method="post">
            {% import "form.twig" as form %}
            {{ form_widget(form) }}

            {#<p>{{ form.input('text', 'name', 'name', 'form-control', 'Nome do Produto', 'required') }}</p>#}
            {#<p>{{ form.input('text', 'descripton', 'descripton', 'form-control', 'Descrição do Produto', 'required') }}</p>#}
            {#<p>{{ form.input('text', 'value', 'value', 'form-control', 'Valor do Produto', 'required') }}</p>#}
            {#<p>{{ form.btn('submit', 'submit', 'submit', 'btn btn-success', 'Cadastrar', 'required', null) }}</p>#}

        </form>







        <table class="table table-striped">
            <thead>
            <tr>
                <th>NOME</th>
                <th>DESCRIÇÃO</th>
                <th>VALOR</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <form action="{{ path('inserir') }}" method="post">
                <td><input type="text" class="form-control" name="name" required="required" required></td>
                <td><input type="text" class="form-control" name="description" required="required" required></td>
                <td><input type="text" class="form-control" name="value" required="required" required></td>
                <td>
                    <input type="hidden" id="id" name="id" />
                    <a href=""><button class="btn btn-success " type="submit" name="cadastrar" >Cadastrar</button></a>
                </td>
                </form>
            </tr>
            </tbody>
        </table>
        <a href="{{ path('lista_page', {page:1 } ) }}"><button class="btn btn-info " type="submit" name="voltar" >Voltar</button></a>
    </div><!-- /jumbotron -->
{% endblock %}